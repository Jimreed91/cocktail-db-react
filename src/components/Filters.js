import Filter from "./Filter"
const Filters = ({handleFilter}) => {

  return(
    <div className="flex">
      {/* <Filter type={"Glass"} searchKey={"g"} handleFilter={handleFilter}/>
      <Filter type={"Alcoholic?"} searchKey={"a"} handleFilter={handleFilter}/> */}
      {/* <Filter id={"buddy"} type={"Category"} searchKey={"c"} handleFilter={handleFilter}/> */}
    </div>
  )
}

export default Filters
